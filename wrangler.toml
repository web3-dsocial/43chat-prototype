name = "world-im"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# Deploy with:
#   npx wrangler pages deploy
#
# Pages Functions in dist/functions/ are auto-detected as Cloudflare Workers
